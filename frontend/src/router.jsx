import {Route, createBrowserRouter, createRouteFromElements} from 'react-router-dom';

const router = createBrowserRouter(
    createRouteFromElements(
        <>
            
        </>
    )
);

export default router;